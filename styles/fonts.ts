import { Archivo, JetBrains_Mono } from 'next/font/google';

export const archivo = Archivo({ subsets: ['latin'], axes: ['wdth'] });
export const jetbrainsMono = JetBrains_Mono({ subsets: ['latin'] });
